# Model generated by scaffold
# Attributes
<% attributes.each do |attribute| %># <%= "#{attribute.name}:#{attribute.type}" %>
<% end %>
class <%= @model_class %> < ActiveRecord::Base
  unloadable
end
